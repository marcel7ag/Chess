package Pieces;

import javax.swing.*;

public class Pawn extends Piece{
    public Pawn(boolean white, String str, ImageIcon img, boolean firstMove){
        super(white, str, img, firstMove);
    }

}