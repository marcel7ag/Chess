package Pieces;

import javax.swing.*;

public class Queen extends Piece{
    public Queen(boolean white, String str, ImageIcon img, boolean firstMove){
        super(white, str, img, firstMove);
    }

}