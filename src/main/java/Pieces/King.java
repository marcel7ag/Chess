package Pieces;

import javax.swing.*;

public class King extends Piece{
    public King(boolean white, String str, ImageIcon img, boolean firstMove){
        super(white, str, img, firstMove);
    }

}