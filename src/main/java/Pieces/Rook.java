package Pieces;

import javax.swing.*;

public class Rook extends Piece{
    public Rook(boolean white, String str, ImageIcon img, boolean firstMove){
        super(white, str, img, firstMove);
    }

}